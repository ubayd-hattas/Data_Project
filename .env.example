# Neon PostgreSQL connection string (serverless driver compatible)
# Get from: https://console.neon.tech → Project → Connection Details
DATABASE_URL=

# Data source mode: json (default) or db
DATA_SOURCE=json
